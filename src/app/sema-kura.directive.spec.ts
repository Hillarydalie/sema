import { SemaKuraDirective } from './sema-kura.directive';

describe('SemaKuraDirective', () => {
  it('should create an instance', () => {
    const directive = new SemaKuraDirective();
    expect(directive).toBeTruthy();
  });
});
