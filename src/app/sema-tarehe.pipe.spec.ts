import { SemaTarehePipe } from './sema-tarehe.pipe';

describe('SemaTarehePipe', () => {
  it('create an instance', () => {
    const pipe = new SemaTarehePipe();
    expect(pipe).toBeTruthy();
  });
});
