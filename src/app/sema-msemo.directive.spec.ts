import { SemaMsemoDirective } from './sema-msemo.directive';

describe('SemaMsemoDirective', () => {
  it('should create an instance', () => {
    const directive = new SemaMsemoDirective();
    expect(directive).toBeTruthy();
  });
});
